type Author {
	id: ID!
	name: String!
	age: Int
}

# Root
extend type Query {
	findAllAuthors: [Author]!
	countAuthors: Long!
}

# Root
extend type Mutation {
	createAuthor(name: String!, age: Int): Author!
}